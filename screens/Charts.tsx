import { Box, Heading, Text, View } from "native-base";

const Charts = () => (
    <View style={{ flex: 1, backgroundColor: '#673ab7' }}>
        <Box>
            <Heading color={'white'}>Charts WIP</Heading>
        </Box>
    </View>
  );

export default Charts